version: '3.6'

services:
  redis:
    restart: always
    image: redis:6.0.8
    ports:
      - 6379:6379
    environment:
      - DEBUG=false
    networks:
      - app
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 3s
      retries: 30